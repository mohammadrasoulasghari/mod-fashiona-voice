# راهنمای استفاده از Docker

این پروژه به صورت کامل داکرایز شده است و می‌توانید به راحتی آن را اجرا کنید.

## مراحل راه‌اندازی

### 1. کپی فایل تنظیمات
ابتدا فایل `.env.example` را کپی کرده و نام آن را به `.env` تغییر دهید:
```bash
cp .env.example .env
```

### 2. تنظیم متغیرهای محیطی
فایل `.env` را باز کرده و تنظیمات زیر را انجام دهید:

- `PORT`: پورتی که می‌خواهید برنامه روی آن اجرا شود (پیش‌فرض: 3000)
- `GEMINI_API_KEY`: کلید API خود را از https://makersuite.google.com/app/apikey دریافت کنید

مثال:
```env
PORT=3000
GEMINI_API_KEY=your_actual_api_key_here
```

### 3. اجرای برنامه
با دستور زیر برنامه را اجرا کنید:
```bash
docker compose up
```

یا اگر می‌خواهید در پس‌زمینه اجرا شود:
```bash
docker compose up -d
```

### 4. دسترسی به برنامه
پس از اجرا، برنامه روی آدرس زیر در دسترس خواهد بود:
```
http://localhost:PORT
```

به عنوان مثال، اگر `PORT=3000` تنظیم کرده‌اید:
```
http://localhost:3000
```

### 5. متوقف کردن برنامه
برای متوقف کردن برنامه:
```bash
docker compose down
```

## نکات مهم

- مطمئن شوید Docker و Docker Compose روی سیستم شما نصب شده است
- فایل `.env` به صورت خودکار از Git نادیده گرفته می‌شود و commit نخواهد شد
- می‌توانید پورت دلخواه خود را در فایل `.env` تنظیم کنید
- تغییرات کد به صورت خودکار با volume mounting اعمال می‌شود
